#pragma once

f64 generate_coords(FILE *flex_out,
                    FILE *ans_out,
                    const Method method,
                    const u64 seed,
                    const u64 n_pairs);
